// Intentionally minimal. Keeps CI green without pretending to test.
// Prints a reminder so we don't forget to add real tests later.
console.log("No unit tests defined yet. Skipping. (Add Vitest/Jest soon.)");
process.exit(0);
