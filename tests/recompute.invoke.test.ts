import test from 'node:test';
import assert from 'node:assert/strict';

test('recompute-profiles invoke shape', async () => {
  // smoke: ensure the code path builds the invoke with the right name
  // if you have a wrapper, import and call it here
  assert.ok(true); // placeholder if no wrapper exists
});
